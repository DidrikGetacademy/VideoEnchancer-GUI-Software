import requests

url = 'https://learnreflects.com/Server/Registration.php'


def register_user(email_entry,password_entry,name_entry):
    email = email_entry
    password = password_entry
    name = name_entry
